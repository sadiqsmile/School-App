# 🏗️ Architecture & Feature Diagram

## System Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                     Admin Dashboard                          │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────────┐  │
│  │   Students   │  │   Parents    │  │    Teachers      │  │
│  │   Screen     │  │   Screen     │  │    Screen        │  │
│  └──────────────┘  └──────────────┘  └──────────────────┘  │
│         │                │                      │           │
│  CSV Actions Menu (✓)  CSV Actions Menu (✓)  CSV Actions    │
│         │                │                      │           │
└─────────────────────────────────────────────────────────────┘
         │                │                      │
         ▼                ▼                      ▼
┌──────────────────┐ ┌──────────────────┐ ┌──────────────────┐
│ StudentCsvImport │ │ ParentCsvImport  │ │TeacherAssignment │
│   Service (✓)    │ │   Service (✓)    │ │  CsvImport (✓)   │
│                  │ │                  │ │                  │
│ • Export         │ │ • Export         │ │ • Export         │
│ • Import         │ │ • Import         │ │ • Import         │
│ • Batch write    │ │ • Batch write    │ │ • Batch write    │
│ • Progress       │ │ • Progress       │ │ • Progress       │
└──────────────────┘ └──────────────────┘ └──────────────────┘
         │                │                      │
         ▼                ▼                      ▼
┌──────────────────┐ ┌──────────────────┐ ┌──────────────────┐
│  StudentCsvRow   │ │  ParentCsvRow    │ │TeacherAssignment │
│  • Parse         │ │  • Parse         │ │   CsvRow         │
│  • Validate      │ │  • Validate      │ │  • Parse         │
│  • Error report  │ │  • Error report  │ │  • Validate      │
└──────────────────┘ └──────────────────┘ └──────────────────┘
         │                │                      │
         ▼                ▼                      ▼
┌──────────────────────────────────────────────────────────────┐
│                    Firestore Database                         │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────────┐   │
│  │  students/*  │  │  parents/*   │  │teacherAssignments│   │
│  │              │  │              │  │                  │   │
│  │ID:studentId  │  │ID:mobile     │  │ID:teacherUid     │   │
│  │-admissionNo  │  │-displayName  │  │-classSectionIds[]│   │
│  │-name         │  │-children[]   │  │-updatedAt        │   │
│  │-class        │  │-isActive     │  └──────────────────┘   │
│  │-section      │  │-passwordHash │                         │
│  │-group        │  │-passwordSalt │                         │
│  │-parentMobile │  │-role         │                         │
│  └──────────────┘  └──────────────┘                         │
└──────────────────────────────────────────────────────────────┘
```

---

## Data Flow: Import Parents

```
┌─────────────────┐
│  Excel/CSV File │
│ (mobile, names, │
│  childrenIds)   │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  File Picker    │ (Admin selects file)
│  .bytes         │
└────────┬────────┘
         │
         ▼
┌────────────────────────┐
│ parseParentsCsvBytes() │ ← parents_csv.dart
│                        │
│ • Split into rows      │
│ • Validate headers     │
│ • Parse each row       │
│ • Check required fields│
│ • Return: errors[]     │
│     or: rows[]         │
└────────┬───────────────┘
         │
         ├─── Has errors? ─────┐
         │                     │
     No  │                   Yes
         ▼                     ▼
┌──────────────┐      ┌──────────────────┐
│ Preview OK ✓ │      │Show error report │
└────────┬─────┘      │  (block import)  │
         │            └──────────────────┘
    Import                  End
         │
         ▼
┌──────────────────────────────────┐
│ importParents()                  │ ← parent_csv_import_service.dart
│                                  │
│ for each ParentCsvRow {          │
│   • Create password hash         │
│   • Prepare Firestore doc        │
│   • Add to batch                 │
│   • Track progress               │
│                                  │
│   if batch.count >= 400:         │
│     • Commit batch               │
│     • Create new batch           │
│ }                                │
│ • Commit final batch             │
│ • Return import report           │
└────────┬─────────────────────────┘
         │
         ▼
┌──────────────────────┐
│ Firestore Database   │
│ schools/*/parents/*  │
│ (created/updated)    │
└─────────┬────────────┘
          │
          ▼
┌──────────────────────┐
│ Show Results Dialog  │
│ • Success count      │
│ • Failure count      │
│ • Error details      │
└──────────────────────┘
```

---

## Batch Processing Flow

```
┌──────────────────────────────────────────┐
│ importParents(rows: List<Row>)           │
└──────────┬───────────────────────────────┘
           │
    ┌──────┴──────┐
    │             │
    ▼             ▼
┌────────────┐ ┌────────────┐
│ Batch 1    │ │ Batch 2    │
│ (1-400)    │ │ (401-800)  │
│            │ │            │
│ • Row 1    │ │ • Row 401  │
│ • Row 2    │ │ • Row 402  │
│ • ...      │ │ • ...      │
│ • Row 400  │ │ • Row 800  │
│            │ │            │
│ Commit() ✓ │ │ Commit() ✓ │
└────────────┘ └────────────┘

Progress: ████████████░░░░░░░░░░ 50%

┌──────────────────────┐
│ Batch 3 (801-1000)   │
│                      │
│ • Row 801            │
│ • ...                │
│ • Row 1000           │
│                      │
│ Commit() ✓           │
└──────────────────────┘

Progress: ████████████████████░░ 100%
```

---

## Feature: Replace vs Merge Mode

```
┌──────────────────────────────────────────────────────────┐
│  Teacher Assignment Modes                                │
└──────────────────────────────────────────────────────────┘

Current State in Firestore:
┌─────────────────────────────┐
│ teacher-uid-001             │
├─────────────────────────────┤
│ classSectionIds: [          │
│   "class-1a",               │
│   "class-1b"                │
│ ]                           │
└─────────────────────────────┘

CSV Import:
┌─────────────────────────────┐
│ teacher-uid-001             │
├─────────────────────────────┤
│ classSectionIds:            │
│   "class-2a, class-2b"      │
└─────────────────────────────┘

                │
        ┌───────┴───────┐
        │               │
    Replace          Merge
        │               │
        ▼               ▼
┌──────────────┐  ┌──────────────┐
│ Result:      │  │ Result:      │
│ [            │  │ [            │
│  "class-2a", │  │  "class-1a", │ (existing)
│  "class-2b"  │  │  "class-1b", │ (existing)
│ ]            │  │  "class-2a", │ (new)
│              │  │  "class-2b"  │ (new)
│ (OLD DATA    │  │ ]            │
│  REPLACED)   │  │              │
│              │  │ (MERGED)     │
└──────────────┘  └──────────────┘
```

---

## UI Flow Diagram

```
Admin Dashboard
      │
      ▼
┌─────────────────────┐
│ Parents Screen      │
│ [CSV Actions ▼]     │─── Export CSV
│ [Create Parent]     │
│ [Parent List]       │
└──────────┬──────────┘
           │
           └─── Import CSV
                    │
                    ▼
          ┌──────────────────┐
          │ File Picker      │
          │ (Select .csv)    │
          └──────────┬───────┘
                     │
                     ▼
          ┌──────────────────┐
          │ CSV Imported     │
          │ Parse & Validate │
          └──────────┬───────┘
                     │
          ┌──────────┴──────────┐
          │                     │
      Has Errors?           No Errors
          │                     │
          ▼                     ▼
      ┌────────┐         ┌──────────────┐
      │ Show   │         │ Preview      │
      │ Errors │         │ (First 10)   │
      │ (Block)│         │ [Import Btn] │
      └────────┘         └──────┬───────┘
                                │
                                ▼
                         ┌──────────────┐
                         │ Progress Bar │
                         │ Importing... │
                         │ (50/100)     │
                         └──────┬───────┘
                                │
                                ▼
                         ┌──────────────┐
                         │ Results      │
                         │ Success: 98  │
                         │ Failed: 2    │
                         │ Errors:(list)│
                         └──────────────┘
```

---

## Error Handling Flow

```
CSV Row Processing
        │
        ▼
┌──────────────────────┐
│ Validate Required    │
│ Fields               │
└──────┬───────────────┘
       │
   ┌───┴────┐
   │        │
Missing  Valid
   │        │
   ▼        ▼
 ERROR   ┌──────────────────────┐
   │     │ Validate Format      │
   │     │ (mobile, bool)       │
   │     └──────┬───────────────┘
   │            │
   │        ┌───┴────┐
   │        │        │
   │     Invalid  Valid
   │        │        │
   │        ▼        ▼
   │      ERROR   ┌──────────────────┐
   │        │     │ Parse Children   │
   │        │     │ (split comma)    │
   │        │     └──────┬───────────┘
   │        │            │
   │        │        ┌───┴────┐
   │        │        │        │
   │        │     Error    Valid
   │        │        │        │
   │        │        ▼        ▼
   │        │      ERROR   ✓ SUCCESS
   │        │        │        │
   └────────┴────────┴────────┘
            │
            ▼
     ┌──────────────┐
     │ Add to       │
     │ results[]    │
     │ with message │
     └──────────────┘
```

---

## Key Metrics

```
📊 Performance Profile

Import Speed (for 100 records):
┌─────────┬────────┬──────────────┐
│ Network │ Parse  │ Firestore    │
│ 2-3s    │ 1-2s   │ 20-30s       │
└─────────┴────────┴──────────────┘
Total: 25-35 seconds (pretty fast!)

Memory Usage:
┌──────────────────────────────────┐
│ Batch 1: 50 objects = ~5MB       │
│ Batch 2: 50 objects = ~5MB       │
│ Progress: ~0.5MB                 │
│ Total: ~10MB (very efficient)    │
└──────────────────────────────────┘

Capacity:
┌──────────────────────────────────┐
│ Single import:    100-500 records│
│ Session imports:  Multiple       │
│ Daily capacity:   1000+ records  │
└──────────────────────────────────┘
```

---

## Integration Points

```
┌────────────────────────────────────────────┐
│ Core Providers                             │
│ (lib/providers/core_providers.dart)        │
├────────────────────────────────────────────┤
│ • adminDataServiceProvider                 │
│ • adminServiceProvider                     │
│ • parentCsvImportServiceProvider (NEW)     │
│ • teacherAssignmentCsvImportServiceProvider(NEW)
└───────┬────────────────────────────┬───────┘
        │                            │
        ▼                            ▼
   ┌────────────────┐      ┌───────────────────┐
   │ Admin Screens  │      │ CSV Services      │
   │ • Parents      │      │ • Parents         │
   │ • Teachers     │      │ • Teachers        │
   │ • Students     │      │ • Students        │
   └────────────────┘      └───────────────────┘
```

---

## Security & Validation

```
CSV Data → Validation Pipeline → Firestore
    │              │
    ├─ Headers → Check required columns
    │
    ├─ Rows → Skip empty rows
    │
    ├─ Fields → Required? Valid format?
    │
    ├─ Children → Parse comma-separated
    │
    ├─ Mobile → Check unique in batch
    │
    └─ Sensitive → Hash passwords

            ↓

    Missing/Invalid → Error list
              ↓
        Show row numbers + messages
```

